from setuptools import setup, find_packages

setup(
    name="disinfotrack",
    version="0.1.0",
    description="Disinformation tracking and analysis toolkit",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[
        # Add your dependencies here
    ],
)
